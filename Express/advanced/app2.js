const express = require("express");

const app = express(); // call createApplication

const router = express.Router; // reference request prototype
const route = express.Route; // reference request prototype
